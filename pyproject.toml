[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis_ai"
version = "0.1.0"
dependencies = [
    "numpy",
    "scikit-learn"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]